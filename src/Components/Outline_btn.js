import '../Assets/Styles/button.scss'

const Button_Ouline = ({name}) => {
    return (
        <span className='btn-ouline'>
            {name}
        </span>
    );
}
 
export default Button_Ouline;